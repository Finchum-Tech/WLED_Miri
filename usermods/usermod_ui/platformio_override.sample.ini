[platformio]
default_envs = esp32dev_usermod_ui

[env:esp32dev_usermod_ui]
extends = env:esp32dev
build_flags = ${common.build_flags} ${esp32.build_flags}
  -D USERMOD_UI
  -D USERMOD_UI_EXAMPLES
lib_deps = ${esp32.lib_deps}
  file://usermods/usermod_ui
